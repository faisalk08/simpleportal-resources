/*
 * Default impplementation of the backbone mobile app, can be customized according to your theme
 * by overriding inside templates folder
 */
// Extends Backbone.Router
define([ "backbonemobileapp" ], function( MobileApp ) {
	var BackboneMobileRouter = MobileApp.extend( {
	    // The Router constructor
		// override any function specific to the app 
		// Backbone.js Routes
	});
	
	// Returns the Router class
	return BackboneMobileRouter;
});
